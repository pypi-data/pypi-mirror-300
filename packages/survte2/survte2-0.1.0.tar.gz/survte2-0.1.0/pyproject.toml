[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "survte2"
version = "0.1.0"
description = "A brief description of survte2"
authors = [{name = "Your Name", email = "your.email@example.com"}]
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.6"

[project.urls]
Homepage = "https://github.com/yourusername/survte2"