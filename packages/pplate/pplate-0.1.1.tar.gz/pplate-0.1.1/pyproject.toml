[tool.poetry]
name = "pplate"
version = "0.1.1"
description = "A CLI to create, manage and use your own boilerplates"
authors = ["hidekimsk <Lucasmsk11@gmail.com>"]
license = "MIT"
readme = "README.md"

[tool.poetry.scripts]
pplate = "pplate.main:app"

[tool.poetry.dependencies]
python = "^3.8"
typer = "^0.12.5"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
