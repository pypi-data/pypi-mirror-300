[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "hst_tools"
authors = [
    {name = "Dmytro", email = "chernousan@gmail.com"}
]
description = "That's all for work"
version = "0.0.8"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []