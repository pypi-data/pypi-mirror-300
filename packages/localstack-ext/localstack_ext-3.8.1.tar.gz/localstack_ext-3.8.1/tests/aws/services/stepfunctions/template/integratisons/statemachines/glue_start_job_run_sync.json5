{
    "Comment": "GLUE_START_JOB_RUN_SYNC",
    "StartAt": "GlueTask",
    "States": {
        "GlueTask": {
            "Type": "Task",
            "Resource": "arn:aws:states:::glue:startJobRun.sync",
            "Parameters": {
                "JobName.$": "$.JobName",
            },
            "End": true
        }
    }
}
