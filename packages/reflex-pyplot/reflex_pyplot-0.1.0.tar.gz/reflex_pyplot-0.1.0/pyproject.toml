[tool.poetry]
name = "reflex-pyplot"
version = "0.1.0"
description = "Reflex custom component for matplotlib."
authors = ["Alek Petuskey <alek@reflex.dev>"]
license = "Apache 2.0"
readme = "README.md"
packages = [
    { include = "pyplot_demo"},
]

[project]
name = "reflex-pyplot"
version = "0.1.0"
description = "Reflex custom component for matplotlib."
author = ["Alek Petuskey <alek@reflex.dev>"]
license = "Apache 2.0"
readme = "README.md"
packages = [
    { include = "pyplot_demo"},
]


[tool.poetry.dependencies]
python = "^3.9"
reflex = "^0.6.1"

[tool.poetry.group.dev.dependencies]
pytest = ">=7.1.2,<8.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
