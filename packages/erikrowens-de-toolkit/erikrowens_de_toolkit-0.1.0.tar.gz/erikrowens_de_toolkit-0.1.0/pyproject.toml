[tool.poetry]
name = "erikrowens-de-toolkit"
version = "0.1.0"
description = ""
authors = ["Erik Owens <erikrowens@gmail.com>"]
readme = "README.md"
packages = [{include = "erikrowens_de_toolkit"}]

[tool.poetry.dependencies]
python = "^3.8"
click = "^8.1.7"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
deng = 'erikrowens_de_toolkit.main:cli'
