[build-system]
requires = [
    "setuptools>=42",
    "wheel",
]

build-backend = "setuptools.build_meta"

[tool.coverage.run]
source = ["ligo"]
omit = [
    "ligo/gracedb/version.py",
    "*/test/*",
    "*/tests/*",
    "*/conftest.py",
]

